# ============================================================================
# DNA ARM: glob400 + DROPKEY, STREAMLINED HIERARCHY. Derived from
# pinball_dna_bidi_linear_flexhier_glob400_dropkey.yaml by TWO keys:
#     hier_downward_refresh:     true -> false
#     hier_upward_refresh_every: 1    -> 4
# (and so from glob400 by three: + local_pack_flex_dropkey 0.1).
#
# WHY DOWNWARD GOES. At 4096 tokens the levels are (4096, 512, 256, 128, 32, 8, 2) and the
# 400-row global block holds whole levels top-down: L6+L5+L4+L3 = 170 rows. Every L0 token reads
# all of them in the same softmax, which is the delivery the downward refresh was for. On the
# record it did little: downward gates decayed 0.1 -> 0.004, refresh projection gains stayed
# init-pinned over 35 DNA epochs, and under a mean control refresh carried +0.017 nats (it acted
# as a learned bias/gain). L1/L2 are NOT in the block: L0 sees them only inside its 156-row band.
#
# WHY UPWARD STAYS (sparser). It is the only path that re-pools DEEP L0 features over a coarse
# row's FULL span. The reverse direction is not direct: a coarse row reads its band (~127 L0
# tokens round its own position), the coarse lane (+-128 coarse slots; its queries are coarse
# only) and the tier/global rows -- an L6 row covers 2048 tokens and samples ~127 per layer.
# Removing upward entirely would leave the top levels holding pooled STEM features plus a thin
# local sample. Every 4 layers is as fast as none (measured, b2 @4096, sm_120, median step):
#     full                     119.7 ms
#     no down                  107.5 ms  -10.2%
#     no down, up every 2      100.6 ms  -16.0%
#     no down, up every 4       97.2 ms  -18.8%   <- this arm
#     no up + no down           99.0 ms  -17.3%   (95.2 on a second run: same, within noise)
# Params -6.30M (the downward projections). flex_union_failed_modules 0 in every row.
# With the downward path gone, hier_lr_mult (notebook) speeds only the upward/level projections
# and gates, so D is now a minor factor in this arm.
#
# READ AGAINST: the 09-24 arm (ep10 .4649 peak, .4612 @ep30), the uncropped control (.4686 @ep25)
# and, if this falls short, pinball_dna_bidi_linear_flexhier_glob400_dropkey.yaml to tell the
# streamlining from the dropout. Readout: in-loop val_SpecificityR (central 6144 bins) + val_loss.
#
# NOTEBOOK SETTINGS FOR THE ARM (not in this file): cross_track_weight = 0, shift_aug_bp = 512 +
# loss_crop_bins = 16352, hier_lr_mult = 1.0; model module cfg_path -> this file.
# hier_lr_mult is a NOTEBOOK setting, not in this file: 1.0 = the legacy optimizer exactly (held off for now); the 09-24 arm ran 3.0.
#
# SCORES (central 6144 bins, running / batch BN):
#     (none yet)
# ============================================================================
# WIDE GLOBAL BLOCK + REGULAR WINDOW. Derived from
# pinball_dna_bidi_linear_flexhier_w16_pool.yaml by flipping TWO keys:
#
#     local_pack_window:       20 -> 156    (regular ~127-token window, not the w16 starve)
#     local_pack_global_block: 64 -> 400    (16 rows -> 336 rows = L6+L5+L4 at T=32768)
#
# THE HYPOTHESIS. Up-down flow needs every L0 row to see the coarse summaries
# DIRECTLY, not via a hop. The global block is a contiguous K/V PREFIX, so its rows
# are gathered to the front before the kernel runs and cost scales with row count,
# not with scatter. Measured 2026-09-21 on the Blackwell (bench_erf, compiled,
# T=32768, fwd+bwd):
#
#     global_block  rows    ms/step    delta
#     64 (current)    16     451.0      --
#     400            336     469.4     +4.1%
#     1400          1360     522.8    +15.9%
#
#   -> 0.013 %/key, flat per row. Compare a per-level RING window (the other way to
#      give L0 coarse reach), measured the same day on the same harness:
#
#     ring [0,4,4,4,4,4,0]    +21.6%      ring [0,8,8,8,8,8,0]  +42.9%
#
#   -> 0.54 %/key, ~40x worse, because a token-radius at level l is STRIDED in packed
#      order: a +-2048-token reach at L5 spans ~79 KV blocks of 64 to collect 8 keys.
#      The prefix is the only block-coherent way to do this. Do not reinstate rings.
#
# WHY 400 AND NOT MORE. Whole-level snapping: the block takes complete levels top-down
# and stops before overflow, so 400 -> 336 (L6+L5+L4) and the next reachable point is
# 1360 (+L3). L3 alone is ~12% of step time, so the cheap triangle tops out at L4.
# The depth AUTO-ADAPTS to sequence length at fixed row count -- at T=8192 the same
# budget reaches L3, at T=2048 it reaches L2 -- which is the desired behaviour.
#
# NOMINATION IS NOT ON HERE. local_pack_global_select stays `levels`. Content selection
# currently REPLACES the block rows rather than adding to them, and its select_every
# cadence measured ANTI-productive at this width (every=32 was ~78 ms/step SLOWER than
# every=1, reproduced under order reversal). Both need resolving before nomination
# rides on a 400-row prefix.
# ============================================================================
# ===================================================================================
# DNA W16 + ATTENTION POOLING  (DNA counterpart of the text flexpool arm)
#
# Parent: pinball_dna_bidi_linear_flexhier_w16.yaml. Diff is the block at the BOTTOM of this
# file only -- hier_pool_mode + local_pack_flex_strict. Everything else byte-identical, so
# this is a ONE-VARIABLE test of the pooling reduction (the parent already has flex_union on,
# which the text flexpool arm did not -- so unlike text, this isolates pooling cleanly).
#
# WHY. _pooled_child_window_means took the MEAN of a parent's children, at the seed AND at
# every upward refresh (hier_upward_refresh is true here, and DNA upward gates were measured
# at 0.62, so it is re-applied ~15 times). A mean is a fine CARRIER and a bad ROUTER: one
# informative child in a comp-wide window is diluted comp-fold before any parent, refresh or
# nominator sees it. attn mode uses softmax(children @ q_l) weights instead, KEEPING the mean
# term so the learnable blend can retreat to it. q_l is ZERO-init, so the pool equals the
# mean EXACTLY at step 0 while d(attn)/dq is nonzero -- identity start, live gradient.
# Cost: (H+1) per coarse level = 6 x 1025 params.
#
# COMPOSES WITH hier_pool_gate, ON in this parent (bias init 2.0). That gate scales each
# child by sigmoid(child . u + b) BEFORE the reduction, then divides by the window COUNT
# (deliberate -- see its docstring). It is a saliency filter; attention pooling is a
# normalised competition among one parent's children. Both apply, gate upstream.
#
# ENV: RUN IN pinball-cs. Measured 2026-09-19: flex fails on EVERY rung of
# _FLEX_TILE_LADDER in ChromScape-bw on sm_120, so a flexhier arm there silently becomes the
# additive arm. local_pack_flex_strict raises instead. Memory: env-card-matrix.
#
# TEXT CONTEXT: the text flexpool arm reached 18.69 val ppl vs a matched dense transformer's
# 20.16 and a window-only control's 26.30 -- but it bundles flex_union WITH pooling, so it
# does not attribute. This arm does.
# ===================================================================================
# Check any edit with:  python bench_hierarchy_plan.py --config <this file> --scan 4096 1048576
block_size: 32768 #4096 #32768 #4096
model_type: pinball
tokenizer_name: gpt2            # unused in features mode; build_pinball stubs it

# Extra levels are comp 8 @ overlap 0.5 -> stride 4. Do NOT use comp 2 here:
# stride = max(1, int(comp*(1-overlap))), so comp 2 @ 0.5 gives int(1.0) = 1 and the level
# does not compress at all.
compression_ratios: [16, 4, 4, 8, 8, 8]
overlap_ratios: [0.5, 0.5, 0.5, 0.5, 0.5, 0.5]

hidden_dim: 1024
num_heads: 16
num_refinement_layers: 15         # LAYER-matched to transformer_dna_bidi, which is also 15.
                                  # Measured: per-layer parameters are the SAME in both models
                                  # (12.87M vs 12.85M -- identical SwiGLU FFN and QKV+out), so
                                  # equal depth means equal per-layer capacity and the ONLY
                                  # difference left is the hierarchy itself. That is the clean
                                  # control; param-matching instead (14 vs 15) would make the
                                  # transformer 25% deeper and confound depth with mechanism.
                                  #
                                  # Residual gap to declare: pinball carries a depth-INDEPENDENT
                                  # 25.59M of scaffolding vs the transformer's 4.33M (its learned
                                  # position table, live even though RoPE is on). At 15 layers
                                  # that is 218.61M vs 197.13M, or 212.04M vs 197.13M (+7.6%)
                                  # counting only parameters that receive gradient -- 6.57M of
                                  # pinball's total is allocated unconditionally and never runs
                                  # here (cond_film, time_embed_mlp, hier_recon_head,
                                  # highest_to_token_projection).
                                  #
                                  # NB full/windowed sit at 12, so this arm is layer-matched to
                                  # the transformer but NOT to the other pinball arms.
num_layers: [0, 0, 0, 0, 0, 0, 0]   # length = level count (L0 + 6 coarse)
internal_cycles: [0, 0, 0, 0]
refinement_style: unified
unified_refinement_cycles: 1
l0_cycles: 0
iterative_refinement_cycles: 0
local_connectivity_window_size: 0
dropout: 0.1
# Per-level DropNode. Index i = level i; L0 is pinned to 0.0 whatever this says. Level sizes
# at 4096 are L1 512, L2 256, L3 128, L4 32, L5 8, L6 2 -- dropping 10% of a 2-row level is a
# 50%-of-the-level event, so the top levels get none.
hier_node_dropout: 0 # 0.1 #0.0
#hier_node_dropout_per_level: [0.0, 0.1, 0.1, 0.05, 0.0, 0.0, 0.0]
norm_type: layernorm
attn_backend: flash             # alias -> l0_local_backend
qkv_sharing: shared
use_gradient_checkpointing: false

# Bidirectional: coverage reads the whole window, nothing to leak.
ar_graph_causal: false
ar_hier_edge_mode: none
drop_static_cross_level_edges: true
local_pack_bidirectional: true
hier_downward_bidi_parent: true

# 1D positions, set explicitly rather than left to the spatial_dims fallback.
spatial_curve: none
local_pack_rope_axial: false

# refine_cond_mode is deliberately absent: the harness calls the model bare, with no
# conditioning vector for per-layer FiLM to read.

upper_init: pooled
upper_seed_gate_init: 1.0
hier_upward_refresh: true      # LEGACY MATCH (was false)
hier_upward_refresh_every: 4    # STREAMLINED ARM: full-span re-pool at layers 4/8/12 only (was: 1)
hier_upward_refresh_gate_init: 0.1
hier_downward_refresh: false    # STREAMLINED ARM: L0 reads L3-L6 directly through the global block (was: true    # LEGACY MATCH (was false))
hier_downward_refresh_every: 1
hier_downward_refresh_gate_init: 0.1
hier_refresh_compile: true
hier_layer_compile: true

# === Stability / reach knobs ==========================================================
# All default off and are exact no-ops when off. Only hier_pool_gate is enabled here.

# Per-child sigmoid relevance gate on hierarchy pooling, shared by the pooled upper seed and
# the upward refresh: pooled = mean_j(sigmoid(<u_l, x_j> + b_l) * x_j). Normalising by the
# window count rather than the gate sum lets an all-low-gate window yield a small vector
# instead of a rescaled average. ~0.1% compute, +6,150 params. Compiled: +1.7% @4096 B=2.
hier_pool_gate: true
# Trade-off, not a formality: ~10 reproduces the plain mean but saturates the gate
# (sigmoid'(10)=4.5e-5) so it never trains. Use ~10 only to warm-start a checkpoint.
hier_pool_gate_bias_init: 2.0

# === Hierarchy stimulation (2026-09-10) ===============================================
# Measured on checkpoint_epoch_5 of the previous run, with a FROZEN coarse-mean control
# (replacement vector captured once per layer/level from a held-out wild-type forward, so
# it cannot depend on the shuffled tile): long-range shuffle sensitivity is entirely
# hierarchy-mediated -- retained 59.8% at 32-48kb, 1.9% at 96-112kb, 0.0% at 240-256kb.
# The model still scores spec 0.4209 / across 0.7721 under that control, so it is not
# broken, it has simply stopped seeing distal sequence. The hierarchy is NOT inert; what is
# weak is the reward (distal influence 0.003 vs 0.024 locally). These two knobs raise the
# gradient pressure on it without touching the task.

# WINDOW DROPOUT: value-side dropout on L0 rows in the packed local attention -- DropNode's
# mirror image (that one is pinned to 0.0 on L0 by design). Thins a query's own local
# neighbourhood so the coarse path has to explain what the window no longer can. Train-only,
# unbiased (values not keys, 1/(1-p) kept), exact no-op at 0.0.
local_window_dropout: 0.0      # LEGACY MATCH (legacy predates this knob)

# COARSE CO-PREDICTION: project each coarse level's summary straight into L0's pre-head
# features through a learnable gate. This is a DIRECT loss->coarse gradient path that does
# not depend on the coarse rows winning attention mass, which is the failure mode the
# retention curve would show as node_spec high + ablation null.
# gate_init 0.0 = exact identity at step 0, so it can only help if it earns its way in.
hier_copredict_l0: false       # LEGACY MATCH (legacy had no copredict)
hier_copredict_levels: [1, 2, 3, 4]
hier_copredict_gate_init: 0.0
# "auto" -> "containing" here, because local_pack_bidirectional is true: each L0 position
# reads the coarse node whose window COVERS it, not the most-recent closed one. The causal
# form would make this the only one-directional pathway in an otherwise two-sided model.
hier_copredict_mode: auto
# ======================================================================================

# Pool L0 into every coarse level directly instead of chaining level <- level-1. The chain
# multiplies per-level factors and repeats every layer (6 levels x 15 layers vs 3 x 12 for
# the windowed arm), and takes six layers for L0 to reach the top. Compiled: +6.2% with the
# gate; pair with hier_upward_refresh_every: 2 to recover it.
hier_upward_refresh_direct: false
hier_upper_seed_direct: false      # same for the one-shot seed (runs once, not per layer)

# Per-level same-level radius, in that level's own node units, filling the resolution gap
# between the mixed-slot band and the global block. Radius 64 reaches +-65/131/262 kb at
# L1/L2/L3 (128 bp/token). Causality verified against a leaking control; lse reproduces the
# single-softmax union to 1.6e-06 relative. Cost at N=32768 B=1, [0,64,64,64,0,0,0],
# compiled (eager understates the relative cost -- the baseline halves, the ring does not):
#   none 470.1ms 33.3GiB | additive +20.1% 39.0GiB | lse +34.9% 46.2GiB | union +82.7% 33.1GiB
# The two-call modes (additive, lse) buy time with memory and add one graph break; union
# gathers nothing extra but costs 4x the time.
local_pack_ring_windows: null
local_pack_ring_merge: additive    # additive | lse | union -- inert while rings are null

# Per-level DropNode. The scalar above drops 10% of every coarse level; at 4096, L5 has 8
# rows and L6 has 2, where that is a large fraction of the level. Index i = level i.
# hier_node_dropout_per_level: [0.0, 0.1, 0.1, 0.05, 0.0, 0.0, 0.0]
# ======================================================================================

# Stability normalizations (both default ON; set false only to reproduce a checkpoint
# trained before they existed). final_norm_fast_path re-applies the terminal norm on exit
# from the refinement loop, which the fast path had lost -- without it nothing renormalizes
# the residual stream before output_projection. qk_norm normalizes q/k over head_dim before
# the dot product, bounding the attention logits; it matters most here because one softmax
# spans all four levels at once. Cost of both together is within noise (~1-2%).
final_norm_fast_path: true
# Standard pre-norm residual stack. ON from the 2026-09-08 run onward.
# ON:  refinement layer (pre-norm norm1/norm2, per layer) -> refresh sublayers each with
#      their own pre-norm -> residual stream untouched -> one terminal final_norm.
# OFF: as ON, plus ONE shared LayerNorm applied to the whole residual stream after every
#      layer -- which breaks the residual identity path, cannot express a depth profile
#      (one gain for all 15 layers), and lands immediately before final_norm at the exit.
# Adds 12 norms / +24,576 params, so it is FRESH-RUN ONLY. Checkpoints from BEFORE
# 2026-09-08 were trained with the legacy stack; they still load and score correctly
# without touching this line (_legacy_norm_stack_fallback detects the missing pre-norms and
# turns the clean stack off for that model), but if you ever need to force it, set false.
# 2026-09-11: SET FALSE -- the stream post-norm goes BACK IN, as the regularizer test.
# Four arms without it all peak early then decline (clean nodrop 0.43626@ep5 -> 0.42833@15;
# noupdownproj 0.42917@ep5; copred+wdrop 0.43683@ep7 -> 0.42081@15), while the one arm WITH
# it climbed to 0.46333@ep15. Train R and specificity are monotonically anti-correlated
# across the variants (0.885/0.463, 0.928/0.428, 0.927/0.425), so the deficit is overfitting
# and the shared post-norm was bounding the stream scale -- an accidental regularizer that
# nothing replaced when it was removed.
# With hier_upward_refresh/hier_downward_refresh BOTH OFF this toggle is CLEAN: no refresh
# pre-norms are allocated in either mode (verified: 406 tensors / 207,946,645 params
# identical both ways, zero key differences), so it moves exactly ONE thing -- the per-layer
# stream post-norm. Every previous legacy-vs-clean comparison also moved the refresh path.
# Known cost, accepted: with refreshes off nothing sits between the last layer's
# pinball_refinement_norm and final_norm, so they run back-to-back and final_norm degrades
# to a learned affine (measured: its input RMS 1.0000, |mu|/sigma 0.000). The 0.46333 arm
# carried the same defect. If this test wins, the follow-up is to skip the post-norm on the
# LAST layer only, or replace it with a per-layer LayerScale/ReZero scalar that damps the
# stream without destroying the residual identity.
# Note the polarity: true = the CLEAN stack = NO extra norm. false = the extra norm is back.
hier_clean_norm_stack: false   # post-norm ON, as legacy had it
# Decouples the other half of the flag: give each refresh projection its OWN pre-norm
# (per level up / per pair down, applied to the pooled rows) WHILE keeping the stream
# post-norm. Not expressible before 2026-09-11. This is the whole point of the arm.
hier_refresh_prenorm: true
# 2026-09-12: drop the pre-norms' learnable gain/bias. The Linear immediately after can
# represent both exactly (Linear(w*xhat+b) = (W.*w)xhat + (Wb+b_lin)), so the affine is
# redundant by construction -- 24,576 params, and a third multiplicative scale in series
# with the Linear and the gate. Measured on this arm at ep35: near pairs damp (gain
# 0.75-0.82) but the TOP pair inverts -- 0:6 gain 1.336 / |bias| 0.376, ~50x the near
# pairs, re-scaling the normalization away and acting as a bias channel (a drift already
# recorded at ep5 and still growing). Zeroing 0:6's affine on the trained checkpoint costs
# only 0.0031 specificity, so little is lost; with this false the route cannot form.
# FRESH-RUN ONLY: removes parameters, so old checkpoints will not load against it.
hier_refresh_prenorm_affine: false

# ONE-KNOB LEVEL COUNT. Truncates/filters every level-indexed list at once
# (compression_ratios, overlap_ratios, num_layers, hier_node_dropout_per_level,
# local_pack_query_levels, local_attn_levels/windows, per_level_*, refresh pairs, ...) so a
# level count can be changed without editing them one by one and silently mis-sizing the
# model when one is missed. Counts COARSE levels: 4 gives L0..L4. null/0 = use the lists as
# written. At N=4096 the levels are [512, 256, 128, 32, 8, 2], so L5/L6 hold 8 and 2 nodes.
hier_level_limit: null     # LEGACY MATCH: 6 coarse levels (L0..L6). The '8 and 2
                           # nodes' note was for N=4096; at N=32768 L5=64 and L6=16.

# SIGREG latent constraint (see _compute_sigreg_loss). Sliced isotropic-Gaussian
# goodness-of-fit per level: forbids both a collapsed direction and a constant offset, which
# a CENTRED VICReg variance hinge cannot see. Training-only, forward untouched (verified
# max|diff| 0.0), no parameters, no checkpoint keys.
# NOTE: the loss is exposed as `_last_sigreg_loss`; the ChromScape notebook currently only
# consumes `_last_hier_aux_loss`, so it must be added there or this trains nothing.
sigreg_enable: false
lambda_sigreg: 0.01
sigreg_levels: auto             # auto (>= sigreg_min_nodes rows) | all | explicit list
sigreg_min_nodes: 32            # with hier_level_limit 4 this covers L1-L4, i.e. every
                                # remaining level -- otherwise L4 becomes the new unconstrained top
sigreg_include_l0: false        # L0 is the token stream, not the hierarchy
sigreg_num_proj: 128
sigreg_knots: 17
sigreg_sample_size: 4096        # per level; caps the [n, proj, knots] intermediate
sigreg_normalize_n: true
sigreg_train_only: true

# WHICH STREAM THE LATENT CONSTRAINTS READ. The refinement stack is pre-norm, so the last
# layer's output is a bare residual sum and `final_norm` outside the loop is the only thing
# that closes the ladder -- it is what output_projection and the CNN head consume. The aux
# losses read tb_graph.x, which is BEFORE that norm, so head and constraints were defined on
# two different representations (measured at init, N=1024: RMS 1.29 at L0, ~4.8 at every
# coarse level, all mapped to 1.0 by final_norm).
# Members: aux | predaux | sigreg ("all" / "none" also accepted). Reuses final_norm, so no
# new parameters and no new checkpoint keys.
# MEASURED, controlled A/B at init (one model, same seed, flag toggled; run-to-run noise
# floor 0.18% on aux and 1.3% on sigreg):
#   hier_aux (cosine) 0.5655 -> 0.5643  (ratio 0.998 -- WITHIN NOISE; cosine is already
#     scale-invariant, so only final_norm's per-row mean subtraction moves it)
#   sigreg            0.5898 -> 0.3066  (ratio 0.52 -- REAL, ~35x the noise floor)
# So aux/predaux are moved for correctness at no measurable cost, and sigreg is deliberately
# NOT moved: LayerNorm centres each row across channels, and not centring is precisely why
# SIGReg was chosen over VICReg (VICReg centres and is therefore blind to a constant-bias
# degeneracy). Normalising first hands the statistic half its value for free and reintroduces
# that blindness. Set to "all" only to test that claim.
hier_latent_exit_norm: [aux, predaux, pc]   # pc ADDED 2026-09-15: PC scores the
                                           # same representation the head reads, as on text
qk_norm: true
qk_norm_type: rms               # rms (nn.RMSNorm) | layer (nn.LayerNorm)

# Required: these populate local_attn_config, which gates the packed cross-level path.
# Without them every local_pack_* key below is silently inert.
local_attn_windows: [128, 128, 128, 128, 128, 128, 128]
# ^^ NOT the effective window: this 7-entry list does not match local_attn_levels (absent
# here, so it defaults to [0], length 1), so the override at
# hierarchical_flow_gat_cached_batch.py:3782 does NOT fire and l0_local_window below wins.
# Verified 2026-09-19: local_attn_config == {0: 16}. Do NOT add local_attn_levels without
# also fixing this list -- matching lengths would silently restore a window of 128.
l0_local_window: 16            # W16 ARM: kept consistent (packed path uses local_pack_window)

local_pack_cross_level: true
local_pack_query_levels: [0, 1, 2, 3, 4, 5, 6]   # MUST cover every level
local_pack_level_bias: true
local_pack_coarse_lane: true
# Lane radius in COARSE bank slots -- this is the knob that buys within-level lateral
# reach, and unlike the band it is denominated in the bank rather than the mixed stream.
# Own-level nodes seen at 32768: 900 -> 982/491/245 at L1/L2/L3, 469 -> 512/256/128,
# 64 -> 70/35/17. Constant in N, because each level's share of the bank is. Must be a
# scalar or "auto": the per-level list form takes a different spec branch (one flash call
# per level, measured 1.8x slower at 4096 -- launch-bound, not FLOP-bound).
local_pack_coarse_window: 128 #900
# The band, the lane and the tier are three separate flash calls summed. Additive is not
# one softmax over the union: a coarse row inside BOTH the band and the lane is attended
# twice (~57 of ~900 lane keys at 32768). That is inherent to the path, not a defect, and
# the trained A/B on 2026-07-14 favoured additive over flex_union late-run anyway.
local_pack_flex_union: true     # THE ONE INTENDED DIFFERENCE from the 3kernel arm
local_pack_global_block: 400    # WIDE GLOBAL: 336 rows = L6+L5+L4 at T=32768; +4.1% measured
# ^ ENABLED 2026-09-17. Measured this session: the GLOBAL TIER is queries = keys = tier rows
# (hierarchical_message_passing.py ~4597), so L0 NEVER reads it -- global context reached a token
# only through hier_downward_refresh, the path the gate audit measured decaying 0.1 -> 0.004.
# That is the likely reason block-shuffle influence at 240-256 kb is ~100x the window-only
# control (the distal signal EXISTS) while the hierarchy bought only ~0.002 specificity over
# window-only at ep5 (it was NOT DELIVERED). This block prepends a fixed top-down coarse budget
# to the K/V of EVERY query, so tokens read a global summary directly.
#   64 -> L4+L5+L6 = 42 rows at 500 kb (4,096 tokens); use 128 at 4 Mb for L5+L6 = 80.
#   FIXED budget keeps it O(N): cost <= budget*N. A sqrt-sized budget would be N^1.5.
# MEASURED FASTER, not slower: 4096 tok fwd+bwd, both orderings, 152.8/151.5 ms off vs
# 141.3/139.5 ms on (-7.8%), memory-neutral -- the flag switches the flex layout to the more
# block-coherent PREFIX form (KV blocks 3768 -> 3138).
# NOTE the flag was a SILENT NO-OP before 2026-09-17: the shared gate was allocated under
# local_pack_coarse_global and the flag was never threaded to the layer. Fixed in
# hierarchical_message_passing.py + hierarchical_flow_gat_cached_batch.py, verified
# bit-identical when off. An older checkout will ignore this key.
# BASELINES PREDATE THIS. Any specificity in the headers below (incl. wdfix 0.44000 ep5 /
# 0.44349 ep10 / 0.44230 ep15) was produced with the block OFF. Use git history to reproduce.
local_pack_coarse_global_gate_init: 0.1   # inert while flex_union is true (the block joins the
                                          # unified softmax ungated); matters only on the
                                          # additive path, where 0.0 would be silent.
local_pack_l0_coarse_rank_window: 0     # OFF: that clause is the quadratic one (its key
                                        # set is a fixed fraction of N).
local_pack_lane_merge: false    # causal-only; inert in a bidi run

# GLOBAL TIER. Whole levels top-down while they fit the budget, attending all-to-all over
# themselves. 400 buys L4+L5+L6 = 336 nodes at 32768, the same global set the 896-row block
# held, for n^2 instead of n*N.
#   budget 0      topmost level alone: 16 nodes at 32768, and only 2 at 4096.
#   budget sqrt   floor(sqrt(N)); at 32768 that is 80 nodes and strands L4, hence 400 here.
local_pack_top_global: true
local_pack_top_global_budget: 400     # 0 | <row budget> | sqrt
# local_pack_coarse_window also accepts "auto": the radius that puts a tier node inside EVERY
# coarse row's window, so the ascent to the tier is one hop. It is sized from the true
# worst-case distance to the nearest tier row, not from mean spacing -- tier levels carry
# large ar_time offsets, so the first tier node sits ~224 bank rows in at 16384 and any
# spacing-derived radius leaves the head of the sequence unable to ascend (measured: 0 tier
# nodes in window for the earliest rows). Under this rule reach measures flat at 5 hops from
# 4k to 512k, where the fixed-budget block above degrades (3 hops at 32k, 9 at 131k, none
# within 10 at 524k) because it holds fewer levels as N grows.
#
# local_pack_coarse_window_tier_nodes (k) widens the window to span about k tier nodes. It
# only binds above the geometric floor: at 16384 with budget sqrt the floor is 268, so k=2..4
# all resolve to 268 and k=10 gives 465, which costs +0.7% -- inside the ~1% run-to-run noise
# on this harness. A wider window is what gives the level just below the tier (L4, span 256)
# a usable receptive field, so raising k is cheap insurance.
#
# Measured at 16384 (compiled, 4090, vs a no-tier windowed lane), both sized to guarantee the
# ascent: tier=40 (budget sqrt) +1.9% at W=268; tier=8 (budget 0) +4.8% at W=1088. A sparse
# tier is the expensive option -- it needs a far wider lane to stay reachable.
#
# HEAD-TO-HEAD vs THIS ARM at 32768 (compiled, Blackwell, pinball-bw, flexfail=0 throughout):
#   flexhier as-is (flex_union + 896 block)   460.8 ms   own-level L1/L2/L3 =  31/ 15/  7
#   budget 400 + coarse_window 900            455.0 ms   own-level         = 982/491/245
#   budget 400 + coarse_window 469            450.2 ms   own-level         = 512/256/128
#   all-to-all lane, no tier                  485.5 ms   own-level = ALL (56.3M pairs, N^2)
# Same 336 global nodes either way; the tier costs n^2 where the block costs n*N (120x apart
# at every length), so the block is the term that cannot scale. Deltas are near the ~1%
# run-to-run noise, and the tier arms ran later/hotter, so they are conservative.

# Source gating is on in every arm so the arms differ by mechanism, not parameter count.
# sigmoid(hqd_source_gate_logit) reads out how much the model weights HQD; dump it from a
# checkpoint with tools_gate_dump.py.
attention_source_gating_enable: false
attention_source_gate_init_graph: 1.0
attention_source_gate_init_local: 1.0   # 1.0, not the 0.5 default, which halves the main
                                        # attention path
attention_source_gate_init_hqd: 0.05    # small but trainable: a sigmoid near 0 damps its
                                        # own gradient and starves the HQD subtree
attention_source_gate_debug: true       # 0-dim tensors, one sync per logging interval

xq_nominate_enable: false
pinball_multirate_enable: false
# HIERARCHY RECONSTRUCTION AUX: each parent is pulled toward the mean of its children
# (targets detached, so the gradient goes into the PARENT only -- "be a summary of your
# children"). This was near-vacuous while hier_upward_refresh was on, because the LAST op
# before the loss was literally `parent += gate * proj(pool(children))` -- the aux scored a
# relationship that had just been installed mechanically. With the refresh off, nothing
# re-pools during refinement, so it becomes a real constraint that attention has to maintain:
# it now TEACHES attention what the refresh was hard-wiring, at zero parameters.
# It is still minimised by COLLAPSE (identical features -> cosine 0), so keep sigreg on as the
# anti-collapse partner; the two are complementary and sigreg is free (it saturates by ep1).

# ----- predictive coding (THE ONE INTENDED DIFFERENCE from the flexhier arm) -----
hier_pc_enable: false          # W16 ARM: no PC, so this matches the wdfix-style hierarchy arm
lambda_hier_pc: 0.01            # 0.01 on 2026-09-15 to DISENTANGLE TWO CHANGES that
                                # landed together. The PC sum finally plummeted (22.5 -> 8.5 in
                                # 914 steps, after sitting at 24-27 for 10,000), but in that same
                                # run lambda went 0.01 -> 0.1 AND the Muon routing fix took
                                # effect, so either could be the cause.
                                # THE MUON FIX: hier_pc_decoder params were being routed to Muon
                                # by the ndim>=2 rule. `up` is 3-D [comp, hidden, rank] and Muon
                                # flattens >2-D updates to [comp, hidden*rank] before
                                # Newton-Schulz, semi-orthogonalising the per-offset decoders
                                # against each other -- while the objective wants adjacent
                                # offsets SIMILAR. Now routed to AdamW in both
                                # ChromScape/bin/optimizer_utils.py and src/pinball/cli.py.
                                # WHY 0.01 AND NOT 0.1: 0.01 is the value that ALREADY FAILED to
                                # move the sum (flat at 24-27 over 10,000 steps) -- but that was
                                # with the decoder on Muon. Rerunning the same lambda with the
                                # routing fixed is the tightest available A/B: one variable, and
                                # a known prior result to compare against.
                                # THE TEST: if PC still plummets at 0.01 with the fix active,
                                # the fix was the cause -- a real finding. If it stalls back at
                                # 24-27 as it did before, the plummet was pressure and the
                                # routing was a minor
                                # handicap. Read train/pc/* to confirm the fix is actually loaded:
                                # those series only appear when the reloaded model module runs.
                                # Previous note: RAISED to 0.1 because at 0.01 the sum would not
                                # fall, so this deliberately applies heavy pressure.
                                # READ WITH hier_aux_cap_frac IN THE NOTEBOOK. 0.1 x ~19 = 1.9
                                # against a supervised loss of ~0.39 is ~490% of the objective --
                                # five times the coverage task, and the observed PC spike of 79.6
                                # (step 1451) would be ~2000% for that step. So any cap below
                                # ~4.9 binds constantly and the CAP, not lambda, sets the true
                                # weight. That is intentional here: cap_frac 1.0 pins PC at 1x the
                                # supervised loss -- well above the ~60% at which text's PC fell
                                # 11x -- while clipping spikes that lambda alone cannot bound.
                                # ABORT SIGNAL: val_loss / val_PearsonR materially worse than the
                                # 0.39098 / 0.664 baseline at epoch 1-2. PC falling while the task
                                # degrades is not a win.
                                # Previous note: RAISED from 0.005 after one epoch at full weight
                                # weight (cap off) moved the PC sum only 24.46 -> 19.01 on
                                # 100-row means over 5800 steps. Text fell ~11x in ~3000 steps,
                                # so this is far slower than the reference, and the reason looks
                                # like pressure: 0.005 x ~19 = 0.095 against a supervised loss of
                                # ~0.39 is ~24% of the objective, where text's fast drop happened
                                # at ~60%. 0.01 puts it near 49%. Headroom confirmed by epoch 0
                                # val: 0.39098 / PearsonR 0.664, against the throttled run's
                                # 0.39004 / 0.66148 -- PC is costing the task nothing so far.
                                # NOT the text arm's 0.25 -- see the LAMBDA note in the header.
                                # The DNA coverage loss is ~0.33 against text CE ~4.2, so the
                                # same weight is ~13x more dominant here, and this arm has 6
                                # pairs to text's 3. 0.005 x an expected ~5.2 steady-state PC
                                # gives ~0.026, about 8% of the supervised loss.
                                # The patched configure_hier_aux_loss reads THIS key, so the yaml
                                # is the single source of truth; the notebook's hier_aux_weight
                                # no longer decides. Re-derive from train/hier_aux_loss once it
                                # has run a few hundred steps -- but NOT from epoch 1.
hier_pc_center_pred: true       # OFFSET-INVARIANT SCORE (added 2026-09-15). The default centres
                                # the denominator but not the numerator, so a shared mean offset
                                # between prediction and target inflates the loss -- exactly what
                                # centring exists to prevent. Measured: the exact per-batch mean
                                # scores 1.000, the same predictor offset by 1 sd scores 2.00 and
                                # by 2 sd 5.02. DNA showed every pair pinned at 3-4, i.e. WORSE
                                # than the mean predictor, which the bias term reaches for free --
                                # so that is a moving target, not insufficient pressure, and no
                                # lambda lifts it. Likely source here: genomic windows differ in
                                # coverage statistics and the host's BatchNorm makes the latents
                                # batch-dependent, so the level's per-batch mean drifts.
                                # NOTE values are NOT comparable across this flag.
hier_pc_center_target: true     # explicit: 1.0 = no better than predicting the level mean.
                                # Uncentred, the denominator carries the level's shared constant,
                                # so inflating that constant lowers the loss for free.
hier_pc_pairs: null             # null = every adjacent (l, l+1): 0:1, 1:2, 2:3
hier_pc_loss_mode: mse_norm     # PC needs magnitude sensitivity, so NOT cosine; mse_norm
                                # bounds the scale that made the original mse aux blow up
hier_pc_l0_target: features     # TASK-ANCHORED -- the clamp is the COVERAGE loss on L0.
                                # NOT `input`: that would capture the host's CNN/UNet features and
                                # make the hierarchy a multiscale generative model of the GENOME
                                # (isochores, repeat density, GC) -- real causes, but causes of
                                # the sequence, with nothing in the objective asking them to be
                                # causes of the coverage. Here input and target are different
                                # modalities, so the choice matters; in the text arm they are the
                                # same stream shifted by one, which is why the TEXT RUN CANNOT
                                # VALIDATE THIS KEY for DNA. With `features` the hierarchy
                                # inherits the task's semantics through L0 without ever naming
                                # the DNA.
hier_pc_l0_ce: false            # no vocabulary here; the L0 term is MSE on those features
hier_pc_detach_target: false    # TRUE PC is joint. true gives the JEPA variant if it destabilises
hier_pc_causal_horizon: auto    # bidirectional -> resolves to 0 (own window). Nothing is
                                # filtered here, so all 16 offsets already participate
                                # (8176 -> 8176 edges, ~32 children per parent) -- the causal
                                # text arm needs the same pairing built geometrically.
hier_pc_own_window_detach: true # WAS false. Flipped after the text arm collapsed with
                                # attached targets (see that config's header). Text cannot
                                # validate the ANCHOR for DNA, but it can validate this flag:
                                # both arms have a soft task-loss clamp, so both are exposed.
# SUPERSEDED by configs/pinball_dna_bidi_linear_3kernel_pc.yaml, which branches off the arm
# actually running (3kernel) rather than flexhier. Kept only if a flexhier PC arm is wanted.
hier_pc_predictor: per_offset   # parent predicts each child INDIVIDUALLY (see header)
hier_pc_rank: 64                # low-rank bottleneck: +3.54M instead of +50.3M full
hier_pc_level_weights: null

use_aux_loss: false
hier_aux_mode: mean_mse         # jepa_mlp would allocate 16.8M predictor params, dead unless aux is on
# MUST be cosine, not the `mse` default: raw mse is scale-dependent and was measured climbing
# 0.1 -> 20, and under hier_clean_norm_stack the stream is no longer renormalized per layer so
# feature norms grow with depth.
hier_aux_loss_mode: cosine
hier_aux_detach_target: true
# Pair weights now follow the LEVEL COUNT (every adjacent pair + L0<-TOP), so hier_level_limit
# is respected instead of the old hardcoded L0-L3 set leaving the top level unregularized.
# At 4 coarse levels: L0<-L1, L1<-L2, L2<-L3, L3<-L4 at 1.0 and L0<-L4 at 0.25.
hier_aux_w_adjacent: 1.0
hier_aux_w_l0_from_top: 0.25
# NOTE: the trainer applies its OWN weight (`hier_aux_weight` in the notebook). 1e-4 was
# copied from a run whose mse-mode aux reached ~20; a cosine aux is O(0.1-0.5), so 1e-4 puts
# it at ~0.01% of the objective, i.e. inert. Use ~0.01 for roughly a 1% share.
lambda_hier_aux: 0.01
token_unet_enable: false
use_edge_embedding: false
learn_edge_from_attention: false
lateral_edge_trace_enable: false

# === DEEP FLEXHIER ARM: windowed L0 + global block, one softmax =======================
# Matches the windowed control's L0 window so the arms differ by hierarchy, not by L0 reach.
# +-W in PACKED slots, NOT tokens: packed length is 5034 at 4096, so 156 slots is ~+-127 L0
# tokens. Do not copy 1248 across from the latent config -- there it happened to equal the
# whole packed length (1024 grid + 224 coarse) and so meant "global"; here it is just a wide
# band that costs 3098 pairs/token instead of 670 and reaches no further.
local_pack_window: 156         # REGULAR window (~127 L0 tokens), NOT the w16 starve
use_hqd: false

# --- added vs the w16 parent -------------------------------------------------------
local_pack_flex_strict: true    # raise rather than silently degrade to additive
# PER-EDGE ATTENTION DROPOUT (the one change in this arm, see header). DropKey: each (query, key)
# score -> -inf with this probability before the softmax, training only; the token-wise V
# dropout from `dropout` above stays on top. 0.0 = off = the parent arm exactly.
local_pack_flex_dropkey: 0.1
hier_pool_mode: attn            # mean | max | attn  (attn == mean exactly at init)
hier_pool_blend_init: 1.0       # 1.0 = pure attention pool; blend stays learnable
